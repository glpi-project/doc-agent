
.. attention::

   This script is only available on **win32**. It is the base script used to setup
   the agent as a service during **win32** installation. You don't normally need
   to run it by itself.

.. hint::

   This script is also handy to setup manually a **win32** service based on a development environment.
